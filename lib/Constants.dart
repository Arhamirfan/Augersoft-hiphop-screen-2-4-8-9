import 'package:flutter/material.dart';

const ktopdecorationShahryar_screen2 = BoxDecoration(
    gradient: LinearGradient(
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
  //todo: change in screen 2 gradient color
  colors: [
    Color(0xff98255b),
    Color(0xff9d2c89),
    Color(0xff852b9b),
    Color(0xff852b9b),
    Color(0xff762aa7),
  ],
));

const ktopdecorationShahryar_screen489 = BoxDecoration(
    gradient: LinearGradient(
  begin: Alignment.bottomLeft,
  end: Alignment.topRight,
  stops: [0.4, 0.6, 0.8, 0.9, 1.0],
  //todo: change in screen 4,8,9 gradient color
  colors: [
    Color(0xff98255b),
    Color(0xff9d2c89),
    Color(0xff852b9b),
    Color(0xff852b9b),
    Color(0xff762aa7),
  ],
));

var kprocessbarValue = 0.2;

const kwhitecolour = TextStyle(color: Colors.white);

//todo: change in text size of top screen title
const kwhitecolourLarge =
    TextStyle(color: Colors.white, fontWeight: FontWeight.bold, fontSize: 25);

const kblackcolour = TextStyle(color: Colors.black);
const kblackbold = TextStyle(color: Colors.black, fontWeight: FontWeight.bold);

//todo: change in text size of title of detailed data
const kblackcolourLarge =
    TextStyle(color: Colors.black, fontWeight: FontWeight.bold, fontSize: 25);
